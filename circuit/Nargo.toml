[package]
name = "pvium_identity"
type = "bin"
authors = ["pvium"]
compiler_version = ">=1.0.0"

[dependencies]
sha256 = { git = "https://github.com/noir-lang/sha256", tag = "v0.3.0" }
base64 = { git = "https://github.com/noir-lang/noir_base64", tag = "v0.5.0" }
